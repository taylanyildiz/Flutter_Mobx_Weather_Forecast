name: flutter_weather_mobx
description: This project simulate Weather Forecast with Provider and Mobx

publish_to: 'none'


version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  provider:
  mobx:
  flutter_mobx:
  mobx_codegen:
  build_runner:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true
